# =============================================================================
# Laravel + MySQL + PostgreSQL(pgvector) + Redis + Nginx
# =============================================================================
#   docker compose up -d
#
# Primary app DB is chosen in .env via DB_CONNECTION. Both servers run always:
#   mysql     -> DB_CONNECTION=mysql   (default)
#   postgres  -> DB_CONNECTION=pgsql
# The Postgres 'vector' database (pgvector) is ALWAYS used for embeddings.
#
# Endpoints:
#   Site ........ http://localhost:8080
#   Horizon ..... http://localhost:8080/horizon
#   MailPit ..... http://localhost:8025
#   phpMyAdmin .. http://localhost:8081
#   RedisInsight  http://localhost:5540
#   MySQL ....... host port 3306
#   PostgreSQL .. host port 5433 (psql -h localhost -p 5433 -U laravel vector)
# =============================================================================

services:

  app:
    build: .
    environment:
      CACHE_STORE: redis
      SESSION_DRIVER: redis
      QUEUE_CONNECTION: redis
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      TELESCOPE_ENABLED: "true"
    volumes:
      - ./:/var/www
      - /var/www/vendor
      - ./docker/entrypoint.sh:/usr/local/bin/entrypoint.sh:ro
    depends_on:
      mysql:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  horizon:
    build: .
    environment:
      CACHE_STORE: redis
      SESSION_DRIVER: redis
      QUEUE_CONNECTION: redis
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      TELESCOPE_ENABLED: "true"
    volumes:
      - ./:/var/www
      - /var/www/vendor
      - ./docker/worker-bootstrap.sh:/usr/local/bin/worker-bootstrap.sh:ro
    depends_on:
      app:
        condition: service_started
      mysql:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    entrypoint: ["worker-bootstrap.sh"]
    command: ["/bin/sh", "-c", "if [ -d /var/www/vendor/laravel/horizon ]; then exec php artisan horizon; else echo 'laravel/horizon not installed — horizon service disabled'; fi"]
    restart: unless-stopped

  scheduler:
    build: .
    environment:
      CACHE_STORE: redis
      SESSION_DRIVER: redis
      QUEUE_CONNECTION: redis
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      TELESCOPE_ENABLED: "true"
    volumes:
      - ./:/var/www
      - /var/www/vendor
      - ./docker/worker-bootstrap.sh:/usr/local/bin/worker-bootstrap.sh:ro
    depends_on:
      app:
        condition: service_started
      mysql:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    entrypoint: ["worker-bootstrap.sh"]
    command: ["php", "artisan", "schedule:work"]
    restart: unless-stopped

  nginx:
    image: nginx:1.31.5-alpine
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    restart: unless-stopped

  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laravel
      MYSQL_USER: laravel
      MYSQL_PASSWORD: secret
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 5s
      retries: 10
    restart: unless-stopped

  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: vector
      POSTGRES_USER: laravel
      POSTGRES_PASSWORD: secret
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/initdb:/docker-entrypoint-initdb.d:ro
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U laravel -d vector"]
      interval: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:8-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 10
    restart: unless-stopped

  mailpit:
    image: axllent/mailpit
    ports:
      - "8025:8025"
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mysql
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  redisinsight:
    image: redis/redisinsight
    ports:
      - "5540:5540"
    environment:
      RI_REDIS_HOST: redis
      RI_REDIS_PORT: "6379"
      RI_REDIS_NAME: app-redis
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  mysql_data:
  redis_data:
  postgres_data:
